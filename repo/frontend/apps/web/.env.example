# Backend endpoint exposed to the browser. Override this in .env.local when needed.
BACKEND_PUBLIC_URL=http://127.0.0.1:3838

# Local Next.js development server.
FRONTEND_HOST=127.0.0.1
FRONTEND_PORT=3000
