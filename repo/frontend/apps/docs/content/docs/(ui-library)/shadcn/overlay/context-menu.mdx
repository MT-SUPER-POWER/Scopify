---
title: Context Menu 上下文菜单
description: 在右键或长按位置显示相关操作。
---

<ComponentPreview name="shadcn-context-menu" />

在右键或长按位置显示相关操作。上方示例直接渲染 `@scopify/ui` 中的真实组件，可以操作并观察其状态变化。

## 安装

该组件已经通过 shadcn CLI 安装在共享 UI 包。需要与上游重新同步时，在仓库根目录运行：

```bash
cd repo/frontend/packages/ui
bunx shadcn@latest add context-menu
```

CLI 文件属于 `shadcn/` vendor 层；产品特有扩展应放在 `scopify/`，不要直接改写原生实现。

## 导入

```tsx
import {
  ContextMenu,
  ContextMenuTrigger,
  ContextMenuContent,
  ContextMenuItem,
  ContextMenuCheckboxItem,
  ContextMenuRadioItem,
  ContextMenuLabel,
  ContextMenuSeparator,
  ContextMenuShortcut,
  ContextMenuGroup,
  ContextMenuPortal,
  ContextMenuSub,
  ContextMenuSubContent,
  ContextMenuSubTrigger,
  ContextMenuRadioGroup,
} from "@scopify/ui/shadcn/components/context-menu";
```

## 用法

下面是上方交互预览的核心结构。业务状态、路由、请求和 i18n 仍由消费层提供。

```tsx
<ContextMenu>
  <ContextMenuTrigger className="bg-muted/40 flex h-40 w-full max-w-sm items-center justify-center rounded-xl border border-dashed text-sm">
    在此区域右键
  </ContextMenuTrigger>
  <ContextMenuContent>
    <ContextMenuItem>立即播放</ContextMenuItem>
    <ContextMenuItem>
      添加到队列 <ContextMenuShortcut>⌘↵</ContextMenuShortcut>
    </ContextMenuItem>
    <ContextMenuSeparator />
    <ContextMenuItem>查看歌曲详情</ContextMenuItem>
  </ContextMenuContent>
</ContextMenu>
```

## 组件结构

```text
ContextMenu
├── ContextMenuTrigger
├── ContextMenuContent
├── ContextMenuItem
├── ContextMenuCheckboxItem
├── ContextMenuRadioItem
├── ContextMenuLabel
├── ContextMenuSeparator
├── ContextMenuShortcut
├── ContextMenuGroup
├── ContextMenuPortal
├── ContextMenuSub
├── ContextMenuSubContent
├── ContextMenuSubTrigger
├── ContextMenuRadioGroup
```

## 常见用法

- 在右键目标附近提供相关操作。
- 支持复选与单选项。
- 关键操作不要只藏在上下文菜单中。

## 使用建议

- 打开后应把焦点移动到浮层，关闭时恢复到触发器。
- 标题与说明用于建立可访问语义；危险操作要明确描述后果。
- 轻量补充用 Popover，聚焦任务用 Dialog，边缘工作区用 Sheet 或 Drawer。

## API 参考

当前模块公开 `ContextMenu`、`ContextMenuTrigger`、`ContextMenuContent`、`ContextMenuItem`、`ContextMenuCheckboxItem`、`ContextMenuRadioItem`、`ContextMenuLabel`、`ContextMenuSeparator`、`ContextMenuShortcut`、`ContextMenuGroup`、`ContextMenuPortal`、`ContextMenuSub`、`ContextMenuSubContent`、`ContextMenuSubTrigger`、`ContextMenuRadioGroup`。实际 props 继承自对应的 Radix primitive 或原生 HTML 元素，并叠加 shadcn 的样式与 slot 约定。

完整的交互模式、可访问性说明和底层 props 请查看 [shadcn/ui Context Menu 上下文菜单 文档](https://ui.shadcn.com/docs/components/radix/context-menu)；实现差异以本仓库当前 CLI 源码为准。
