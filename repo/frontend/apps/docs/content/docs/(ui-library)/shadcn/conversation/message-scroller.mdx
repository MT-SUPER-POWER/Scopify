---
title: Message Scroller 消息滚动器
description: 管理消息列表滚动位置、可见性与回到底部操作。
---

<ComponentPreview name="shadcn-message-scroller" />

管理消息列表滚动位置、可见性与回到底部操作。上方示例直接渲染 `@scopify/ui` 中的真实组件，可以操作并观察其状态变化。

## 安装

该组件已经通过 shadcn CLI 安装在共享 UI 包。需要与上游重新同步时，在仓库根目录运行：

```bash
cd repo/frontend/packages/ui
bunx shadcn@latest add message-scroller
```

CLI 文件属于 `shadcn/` vendor 层；产品特有扩展应放在 `scopify/`，不要直接改写原生实现。

## 导入

```tsx
import {
  MessageScrollerProvider,
  MessageScroller,
  MessageScrollerViewport,
  MessageScrollerContent,
  MessageScrollerItem,
  MessageScrollerButton,
  useMessageScroller,
  useMessageScrollerScrollable,
  useMessageScrollerVisibility,
} from "@scopify/ui/shadcn/components/message-scroller";
```

## 用法

下面是上方交互预览的核心结构。业务状态、路由、请求和 i18n 仍由消费层提供。

```tsx
null;
```

## 组件结构

```text
MessageScrollerProvider
├── MessageScroller
├── MessageScrollerViewport
├── MessageScrollerContent
├── MessageScrollerItem
├── MessageScrollerButton
```

## 常见用法

- 自动跟随新增消息。
- 用户向上阅读时暂停自动滚动。
- 提供明确的回到底部按钮。

## 使用建议

- 消息数据、流式状态和反馈请求保留在消费层，组件只负责展示与交互结构。
- 为动态新增内容维护稳定的焦点和滚动位置，避免打断正在阅读的用户。
- 头像、时间和状态是辅助信息，正文应保持最高阅读优先级。

## API 参考

当前模块公开 `MessageScrollerProvider`、`MessageScroller`、`MessageScrollerViewport`、`MessageScrollerContent`、`MessageScrollerItem`、`MessageScrollerButton`、`useMessageScroller`、`useMessageScrollerScrollable`、`useMessageScrollerVisibility`。实际 props 继承自对应的 Radix primitive 或原生 HTML 元素，并叠加 shadcn 的样式与 slot 约定。

完整的交互模式、可访问性说明和底层 props 请查看 [shadcn/ui Message Scroller 消息滚动器 文档](https://ui.shadcn.com/docs/components/radix/message-scroller)；实现差异以本仓库当前 CLI 源码为准。
